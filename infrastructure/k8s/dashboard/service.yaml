apiVersion: v1
kind: Service
metadata:
  name: qe-dashboard
  namespace: quantum-edge
  labels:
    app: qe-dashboard
    component: dashboard
spec:
  type: ClusterIP
  selector:
    app: qe-dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
