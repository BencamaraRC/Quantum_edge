apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-04-momentum-bot
  namespace: quantum-edge
  labels:
    app: agent-04-momentum-bot
    component: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-04-momentum-bot
  template:
    metadata:
      labels:
        app: agent-04-momentum-bot
        component: agent
    spec:
      containers:
        - name: agent-04-momentum-bot
          image: ${ECR_AGENT_URL}:latest
          env:
            - name: AGENT_NAME
              value: agent_04_momentum_bot
            - name: AGENT_MODULE
              value: agents.agent_04_momentum_bot.main
          envFrom:
            - configMapRef:
                name: qe-config
            - secretRef:
                name: qe-secrets
          resources:
            requests:
              cpu: 128m
              memory: 256Mi
            limits:
              cpu: 256m
              memory: 512Mi
      restartPolicy: Always
